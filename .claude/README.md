# Claude layer

Start at [CLAUDE.md](../CLAUDE.md) (auto-loaded). Files here are read on demand.

## Structure

- `docs/` — architecture, codebase-map, constants-and-options, conventions, common-changes
- `commands/autocode.md` — `/autocode` optimization loop
- `guidelines/` — refactoring, performance, testing, constants, readme-generation
- `progress.md` + `results.tsv` — autocode session state

Plain Markdown with optional `name`/`description` frontmatter. Claude Code-native — do
not reintroduce `*.instructions.md` conventions.
